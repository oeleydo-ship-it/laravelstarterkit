@extends('layouts.superadmin')
@section('title', 'Manage Workspace')
@section('content')
<form method="POST" action="{{ route('superadmin.tenants.update',$tenant) }}">@csrf @method('PUT')<div class="card stat-card mb-4"><div class="card-body p-4"><h5 class="mb-3">General & Plan</h5><div class="row g-3"><div class="col-md-6"><label class="form-label">Name</label><input class="form-control" name="name" required value="{{ old('name',$tenant->name) }}"></div><div class="col-md-6"><label class="form-label">Slug</label><input class="form-control" name="slug" required value="{{ old('slug',$tenant->slug) }}"></div><div class="col-md-6"><label class="form-label">Plan</label><select name="plan_id" class="form-select"><option value="">No plan</option>@foreach($plans as $plan)<option value="{{ $plan->id }}" @selected($tenant->plan_id==$plan->id)>{{ $plan->name }}</option>@endforeach</select></div><div class="col-md-6"><label class="form-label">Trial ends</label><input type="date" name="trial_ends_at" class="form-control" value="{{ old('trial_ends_at',$tenant->trial_ends_at?->format('Y-m-d')) }}"></div></div></div></div><div class="card stat-card"><div class="card-body p-4"><h5>Module Control</h5><p class="text-muted">Override the enabled feature set for this workspace.</p><div class="row g-3">@foreach($modules as $module)<div class="col-md-6"><div class="border rounded p-3 h-100"><div class="form-check form-switch"><input class="form-check-input" type="checkbox" name="modules[]" value="{{ $module->key }}" id="module_{{ $module->key }}" @checked(in_array($module->key,$enabledModules))><label class="form-check-label fw-semibold" for="module_{{ $module->key }}">{{ $module->name }}</label></div><div class="small text-muted mt-1">{{ $module->description }}</div></div></div>@endforeach</div><button class="btn btn-primary mt-4">Save Workspace</button> <a class="btn btn-outline-secondary mt-4" href="{{ route('superadmin.tenants.index') }}">Cancel</a></div></div></form>
@endsection
