@extends('layouts.app')
@section('title', 'Moderate Reviews')
@section('content')
@include('modules.reviews._nav')
<div class="d-flex justify-content-between align-items-center mb-3"><h4 class="fw-bold">Moderate reviews</h4><form><select name="status" class="form-select form-select-sm" onchange="this.form.submit()"><option value="">All statuses</option>@foreach(['pending','approved','rejected'] as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ ucfirst($status) }}</option>@endforeach</select></form></div>
<div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Author</th><th>Rating</th><th>Review</th><th>Status</th><th></th></tr></thead><tbody>@forelse($reviews as $review)<tr><td><strong>{{ $review->author_name }}</strong><br><small class="text-muted">{{ $review->author_company }}</small></td><td>{{ str_repeat('★', $review->rating) }}</td><td>{{ $review->body }}</td><td><span class="badge bg-secondary">{{ ucfirst($review->status) }}</span></td><td class="text-nowrap">@can('update',$review) @if($review->status !== 'approved')<form class="d-inline" method="POST" action="{{ route('reviews.approve',$review) }}">@csrf @method('PUT')<button class="btn btn-sm btn-success">Approve</button></form>@endif @if($review->status !== 'rejected')<form class="d-inline" method="POST" action="{{ route('reviews.reject',$review) }}">@csrf @method('PUT')<button class="btn btn-sm btn-outline-secondary">Reject</button></form>@endif @endcan @can('delete',$review)<form class="d-inline" method="POST" action="{{ route('reviews.destroy',$review) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form>@endcan</td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-4">No reviews found.</td></tr>@endforelse</tbody></table></div></div>{{ $reviews->links() }}
@endsection
