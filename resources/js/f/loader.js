import '../../sass/f/public.scss';
(() => {
 const x=window.__F;if(!x)return;
 const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 const field=f=>{const n=esc(f.key),l=`<label>${esc(f.label)}${f.required?' *':''}</label>`;if(f.type==='textarea')return `<div class="f-field">${l}<textarea name="${n}" ${f.required?'required':''}></textarea></div>`;if(f.type==='select')return `<div class="f-field">${l}<select name="${n}" ${f.required?'required':''}><option value="">Choose…</option>${(f.options||[]).map(o=>`<option>${esc(o)}</option>`).join('')}</select></div>`;if(f.type==='rating'||f.type==='nps')return `<div class="f-field">${l}<div class="f-score">${Array.from({length:f.type==='nps'?11:5},(_,i)=>`<label><input type="radio" name="${n}" value="${i+(f.type==='nps'?0:1)}" ${f.required&&i===0?'required':''}>${i+(f.type==='nps'?0:1)}</label>`).join('')}</div></div>`;return `<div class="f-field">${l}<input type="${f.type==='email'?'email':'text'}" name="${n}" ${f.required?'required':''}></div>`};
 fetch(x.b+'/c').then(r=>r.json()).then(d=>document.querySelectorAll('[data-f-form]').forEach(el=>{const id=el.dataset.fForm, f=d.i.find(v=>String(v.id)===id)||d.i[0];if(!f)return;el.innerHTML=`<div class="f-root" style="--f-color:${esc(d.c||x.c)}"><form class="f-card"><h3 class="f-title">${esc(f.n)}</h3>${f.f.map(field).join('')}<input class="f-hp" name="website" tabindex="-1" autocomplete="off"><button class="f-btn">Submit</button></form></div>`;el.querySelector('form').addEventListener('submit',e=>{e.preventDefault();const a=Object.fromEntries(new FormData(e.target));fetch(x.b+'/s',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({i:f.id,answers:a,page_url:location.href})}).then(r=>r.json()).then(v=>{if(v.ok)e.target.innerHTML=`<p>${esc(v.thank_you||'Thank you.')}</p>`;});});}));
})();
